Update the homepage hero headline in `src/routes/index.tsx` from the current tagline to:

**"Product Analyst and Manager who treats data like dig sites"**

The existing sentence spans lines 68-73. Replace it with the new headline text, preserving the surrounding styling (`<h1>` and `<span className="text-primary">` on "dig sites"). No other files change since the scope is homepage hero only.